# make-struct

To install dependencies:

```bash
npm install
```

To run:

```bash
npx tsx index.ts
```

The script prompts for the model name and API version, then generates the files from the templates.
